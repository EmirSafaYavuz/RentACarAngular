import { BrandOptionFilterPipe } from './brand-option-filter.pipe';

describe('BrandOptionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BrandOptionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
